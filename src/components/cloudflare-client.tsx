"use client";
import { useEffect,useState,type FormEvent } from "react";
import { apiFetch } from "@/lib/client-api";
import { PageHeading } from "./page-heading";
type Status={configured:boolean;enabled:boolean;running:boolean;tunnelId:string|null;dashboardHostname:string|null};
export function CloudflareClient(){const[status,setStatus]=useState<Status|null>(null);const[error,setError]=useState("");const[busy,setBusy]=useState("");async function load(){try{setStatus(await apiFetch<Status>("/api/cloudflare/status"));setError("")}catch(e){setError(e instanceof Error?e.message:"Load failed")}}useEffect(()=>{void load()},[]);async function configure(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy("configure");const f=new FormData(e.currentTarget);try{setStatus(await apiFetch<Status>("/api/cloudflare/configure",{method:"POST",body:JSON.stringify({accountId:f.get("accountId"),zoneId:f.get("zoneId"),apiToken:f.get("apiToken"),tunnelName:f.get("tunnelName"),dashboardHostname:f.get("dashboardHostname")})}));}catch(c){setError(c instanceof Error?c.message:"Configuration failed")}finally{setBusy("")}}async function toggle(){setBusy("toggle");try{setStatus(await apiFetch<Status>(`/api/cloudflare/${status?.enabled?"disable":"enable"}`,{method:"POST"}))}catch(c){setError(c instanceof Error?c.message:"Tunnel action failed")}finally{setBusy("")}}async function sync(){setBusy("sync");try{setStatus(await apiFetch<Status>("/api/cloudflare/sync",{method:"POST"}))}catch(c){setError(c instanceof Error?c.message:"Route sync failed")}finally{setBusy("")}}return <><PageHeading title="Cloudflare Tunnel" description="Optional outbound-only public access. LAN hosting works without Cloudflare; enable it later to assign HTTPS hostnames to selected applications." actions={status?.configured?<><button disabled={!!busy} className="btn" onClick={()=>void sync()}>Sync routes</button><button disabled={!!busy} className={`btn ${status.enabled?"btn-warning":"btn-primary"}`} onClick={()=>void toggle()}>{status.enabled?"Disable tunnel":"Enable tunnel"}</button></>:undefined}/>{error&&<div className="alert alert-error mb-5">{error}</div>}<div className="grid gap-5 xl:grid-cols-2"><form onSubmit={configure} className="card border border-base-300 bg-base-100"><div className="card-body"><h2 className="card-title">{status?.configured?"Update connection":"Connect Cloudflare"}</h2><p className="text-sm text-base-content/65">Use a least-privilege API token restricted to the chosen account and zone.</p><label className="form-control"><span className="label-text mb-1">Account ID</span><input required name="accountId" className="input input-bordered font-mono"/></label><label className="form-control"><span className="label-text mb-1">Zone ID</span><input required name="zoneId" className="input input-bordered font-mono"/></label><label className="form-control"><span className="label-text mb-1">API token</span><input required name="apiToken" type="password" className="input input-bordered"/></label><label className="form-control"><span className="label-text mb-1">Tunnel name</span><input required name="tunnelName" defaultValue="nixhost" className="input input-bordered"/></label><label className="form-control"><span className="label-text mb-1">Dashboard hostname (optional)</span><input name="dashboardHostname" placeholder="console.example.com" className="input input-bordered"/></label><div className="card-actions justify-end"><button disabled={busy==="configure"} className="btn btn-primary">{busy==="configure"?<span className="loading loading-spinner"/>:"Save and create tunnel"}</button></div></div></form><div className="card border border-base-300 bg-base-100"><div className="card-body"><h2 className="card-title">Tunnel status</h2>{!status?<span className="loading loading-spinner"/>:<div className="space-y-3"><div className="flex justify-between"><span>Configured</span><span className={`badge ${status.configured?"badge-success":"badge-ghost"}`}>{String(status.configured)}</span></div><div className="flex justify-between"><span>Enabled</span><span className={`badge ${status.enabled?"badge-success":"badge-ghost"}`}>{String(status.enabled)}</span></div><div className="flex justify-between"><span>Process</span><span className={`badge ${status.running?"badge-success":"badge-ghost"}`}>{status.running?"running":"stopped"}</span></div><div><div className="text-sm text-base-content/55">Tunnel ID</div><div className="font-mono text-sm break-all">{status.tunnelId||"—"}</div></div><div><div className="text-sm text-base-content/55">Dashboard hostname</div><div>{status.dashboardHostname||"Not exposed"}</div></div><div className="alert alert-warning"><span>Protect the dashboard hostname with Cloudflare Access as an additional layer. NixHost login remains required.</span></div></div>}</div></div></div></>}
