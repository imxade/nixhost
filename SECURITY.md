# Security Policy

Do not file public issues containing credentials, private repository URLs, setup tokens, logs with secrets, or host addresses. Report security problems privately to the repository owner until a dedicated security contact is published.

The project is pre-release. No version currently has a security-support guarantee. See `docs/SECURITY.md` for the threat model and `PROJECT_STATUS.md` for incomplete release gates.
